extends layouts/base

block content

  header#head.secondary
  // container
  .container
    ol.breadcrumb
      li
        a(href='/') Home
      li.active Forgot password
    .row
      // Article main content
      article.col-xs-12.maincontent
        header.page-header
          h1.page-title Forgot password
        .col-md-6.col-md-offset-3.col-sm-8.col-sm-offset-2
          .panel.panel-default
            .panel-body
              h3.thin.text-center Forgot password your account
              p.text-center.text-muted
                | Vous avez vos identifiants, 
                a(href='/signin') Login
                |  connectez vous.
              hr

              if (error)
                p.error #{error.message}

              form(action='/forgot_password' method='POST')
                .top-margin
                  label
                    | Email 
                    span.text-danger *
                  input.form-control(type='email', name='email', aria-describedby='emailHelp', required, autocomplete="false")
                hr
                .row
                  .col-lg-8
                    
                  .col-lg-4.text-right
                    button.btn.btn-action(type='submit') Sign in
      // /Article
  // /container

  footer#footer

block scripts
  script(src='http://ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js')
  script(src='http://netdna.bootstrapcdn.com/bootstrap/3.0.0/js/bootstrap.min.js')
  script(src='/lib/headroom.min.js')
  script(src='/lib/jQuery.headroom.min.js')
  script(src='/lib/template.js')
